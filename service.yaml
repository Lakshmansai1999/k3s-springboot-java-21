apiVersion: v1
kind: Service
metadata:
  name: my-java-service
spec:
  type: NodePort
  selector:
    app: my-java-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
